<h2 >@Model.CurrentCategory</h2>
<div class="row">
@{
    foreach (Carpart carpart in Model.Carparts)
    {
        @Html.Partial("CarpartsSummary", carpart)

        @*<div class="col-sm-4 col-lg4 col-md-4">
            <div class="thumbnail">
                <img src="@carpart.ImageThumbnailUrl" alt=""/>
                <div class="caption">
                    <h3 class="pull-right">@carpart.Price.ToString()</h3>
                    <h3>
                        <a>@carpart.Name</a>
                    </h3>
                </div>
            </div>
        </div>*@
    }
}
</div>