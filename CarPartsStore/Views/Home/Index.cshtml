@model HomeViewModel
<div class="py-5 text-center">
    <h2>Детали в наличии</h2>
</div>
<div class="row">
    @foreach (var carpart in Model.IsInStock)
    {
        @Html.Partial("CarpartsSummary", carpart)
    }
</div>